version: 1.1.0
date: 2026-09-19
prompt: docs/prompts/0001-portfolio-refresh-and-proto3d.md
intent: Justin: bring the portfolio up to date and add https://arthovis-org.github.io/empty1/ (Proto3D) plus any other live links in that repo, before the arthovis-org GitHub connection exists.
decision: (1) External repos are added by a committed file, `data/sources.json` (`extra_repos`, `users`), unioned with the existing env vars in `scripts/build-data.mjs` and recorded under `sources.file` in the output, so a repo outside the org can be added with a git commit. (2) `findOverride` accepts a `full_name` key (`owner/repo`) that wins over the bare name, so cross-org name collisions (both orgs use `emptyN`) cannot mis-apply; bare keys and `renamed_from` keep working. (3) An override's `links.live` is used as the live-check URL when neither the Pages API (404 for repos the token cannot administer) nor the `has_pages` guess yields one. (4) Screenshots of repos from another owner are keyed `owner--repo` (`screenshotName()` shared by build-data and screenshot.mjs). (5) `assets/app.js` shows an owner pill on external cards, extends the header line with the external count and searches `full_name`. (6) Overrides refreshed: Proto3D under `arthovis-org/empty1` with live / demo (Gateway Credits) / docs (architecture) links; CTL OS, Petrock featured; Lead Magnet, paperos-template, paperos-orchestrator, Hook Testers (category Other, the derivation hit "tool"), Forge Engine, Llave OS; stale notes for circle-shell, webflow-audit, hoy fixed; sendr-rebuild note; orphan empty10 override + screenshots removed. (7) Docs scaffolding started at 0001 with the hoy pattern; `CLAUDE.md` added.
rejected: Setting `PORTFOLIO_EXTRA_REPOS` as an Actions variable — not reachable by git, and invisible in the repo history. Hand-editing `data/projects.json` to insert Proto3D — it is a generated file; the Action regenerates it on this push. Keying external screenshots by bare name — a same-named repo in two orgs would overwrite the other's capture. Fetching arthovis-org through the GitHub API locally — proxy-gated in the sandbox; the research used the public Pages host and github.com pages instead.
files: scripts/build-data.mjs scripts/screenshot.mjs assets/app.js data/sources.json data/overrides.json data/projects.json screenshots/empty10.png screenshots/thumbs/empty10.png package.json package-lock.json README.md CLAUDE.md docs/README.md docs/rules/documentation.md docs/prompts/0001-portfolio-refresh-and-proto3d.md docs/changelog/0001-portfolio-refresh-and-proto3d.md docs/decisions.md docs/kanban.md docs/reference/surfaces.md docs/pages/HOME.md

## Verification
- `node --check` on `scripts/build-data.mjs`, `scripts/screenshot.mjs`, `assets/app.js`: clean. `JSON.parse` of `data/overrides.json` and `data/sources.json`: ok.
- `node scripts/build-data.mjs --recompute`: exit 0, no warnings; changes: `empty12` Product → Tooling, `empty-11` Prototype / Shell rebuild → Tooling, `cal-tenant-law` Game → Product, `empty6` Placeholder → Other; 23 projects, status counts unchanged (17 live, 1 deployed-unverified, 2 in-progress, 3 placeholder).
- Unit check of the exported helpers: `findOverride` returns the `arthovis-org/empty1` entry over a bare `empty1` key and still resolves bare and `renamed_from` keys; `screenshotName` gives `arthovis-org--empty1` for the external repo and `hoy` for an org repo; `finalizeProject` on a Proto3D-shaped entry yields `live / Product / Proto3D`.
- Local Playwright load of the page (Chromium from `/opt/pw-browsers`, 1280 wide): see the summary in the prompt's response and the commit body; capture saved outside the repo (`scratchpad/portfolio-after.png`).
- Not verified here: the Action's live run against `arthovis-org/empty1` (GitHub API is proxy-gated in the sandbox); checked afterwards by polling the deployed `data/projects.json`.
